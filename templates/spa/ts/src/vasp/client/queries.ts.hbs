// Auto-generated by Vasp — do not edit
import { createVaspClient } from '@vasp-framework/runtime'
import type {
{{#each queries}}
  {{pascalCase name}}Args,
  {{pascalCase name}}Return,
{{/each}}
} from './types.js'

// Module-level singleton — safe to call from any context (event handlers, stores, etc.)
const _client = createVaspClient({ baseURL: (import.meta.env.VITE_API_URL as string) || '/api' })

{{#each queries}}
/**
 * Call the '{{name}}' query on the Vasp backend.
 */
export async function {{camelCase name}}(args?: {{pascalCase name}}Args): Promise<{{pascalCase name}}Return> {
  return _client.query('{{camelCase name}}', args) as Promise<{{pascalCase name}}Return>
}

{{/each}}
