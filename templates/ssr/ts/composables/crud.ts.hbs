// Auto-generated by Vasp — do not edit directly
// SSR-aware typed CRUD composables — wrap $fetch with backend URL, auth headers, and CSRF handling.
// On the server, request cookies are forwarded to the Elysia backend for session continuity.
import { $fetch } from 'ofetch'
import { safeParse } from 'valibot'
import type {
{{#each cruds}}
  {{pascalCase entity}},
{{#if (includes operations "create")}}
  Create{{pascalCase entity}}Input,
{{/if}}
{{#if (includes operations "update")}}
  Update{{pascalCase entity}}Input,
{{/if}}
{{/each}}
} from '@shared/types.js'
import {
{{#each cruds}}
{{#if (includes operations "create")}}
  Create{{pascalCase entity}}Schema,
{{/if}}
{{#if (includes operations "update")}}
  Update{{pascalCase entity}}Schema,
{{/if}}
{{/each}}
} from '{{importPath "@shared/validation.js" ext}}'

/** Unwrap the server's `{ ok, data }` envelope. Throws on `ok: false`. */
function unwrap<T>(res: unknown): T {
  if (res && typeof res === 'object' && 'ok' in res) {
    const env = res as { ok: boolean; data?: T; error?: { message?: string } }
    if (!env.ok) throw new Error(env.error?.message ?? 'Request failed')
    return env.data as T
  }
  return res as T
}

{{#each cruds}}
export function use{{pascalCase entity}}Crud() {
  const config = useRuntimeConfig()
  const baseURL = config.public.apiBase as string
  const base = `${baseURL}/crud/{{camelCase entity}}`

  // On the server, forward the incoming request's cookies so the backend can authenticate
  const forwardHeaders = import.meta.server ? useRequestHeaders(['cookie']) : {}

  const loading = ref(false)
  const error = ref<string | null>(null)

{{#if (or (includes operations "create") (includes operations "update") (includes operations "delete"))}}
  function getCsrfToken(): string | undefined {
    if (import.meta.server) return undefined
    const match = document.cookie.match(/(?:^|;\s*)vasp-csrf=([^;]*)/)
    return match?.[1] !== undefined ? decodeURIComponent(match[1]) : undefined
  }

{{/if}}
  const fail = (err: unknown) => {
    error.value = err instanceof Error ? err.message : 'Request failed'
  }

  return {
    loading,
    error,
{{#if (includes operations "list")}}
    list: async (params?: {
{{#if listConfig}}
{{#if listConfig.paginate}}
      page?: number
{{else}}
      offset?: number
{{/if}}
      limit?: number
{{#if listConfig.sortable}}
      sortBy?: {{#each listConfig.sortable}}'{{this}}'{{#unless @last}} | {{/unless}}{{/each}}
      dir?: 'asc' | 'desc'
{{/if}}
{{#if listConfig.filterable}}
      filter?: Partial<Record<{{#each listConfig.filterable}}'{{this}}'{{#unless @last}} | {{/unless}}{{/each}}, string>>
{{/if}}
{{#if listConfig.search}}
      search?: string
{{/if}}
{{else}}
      limit?: number
      offset?: number
      orderBy?: string
      dir?: string
{{/if}}
    }): Promise<{
      items: {{pascalCase entity}}[]
      total: number
{{#if listConfig.paginate}}
      page: number
      limit: number
{{else}}
      limit: number
      offset: number
{{/if}}
    }> => {
      loading.value = true
      error.value = null
      try {
        const query: Record<string, string | number | undefined> = {}
        if (params?.limit !== undefined) query.limit = params.limit
{{#if listConfig.paginate}}
        if ((params as { page?: number } | undefined)?.page !== undefined) query.page = (params as { page?: number }).page
{{else}}
        if ((params as { offset?: number } | undefined)?.offset !== undefined) query.offset = (params as { offset?: number }).offset
{{/if}}
{{#if listConfig.sortable}}
        if ((params as { sortBy?: string } | undefined)?.sortBy) query.sortBy = (params as { sortBy?: string }).sortBy
        if ((params as { dir?: string } | undefined)?.dir) query.dir = (params as { dir?: string }).dir
{{else}}
        if ((params as { orderBy?: string } | undefined)?.orderBy) query.orderBy = (params as { orderBy?: string }).orderBy
        if ((params as { dir?: string } | undefined)?.dir) query.dir = (params as { dir?: string }).dir
{{/if}}
{{#if listConfig.filterable}}
        if ((params as { filter?: Record<string, string | undefined> } | undefined)?.filter) {
          for (const [k, v] of Object.entries((params as { filter: Record<string, string | undefined> }).filter)) {
            if (v !== undefined) query[`filter.${k}`] = v
          }
        }
{{/if}}
{{#if listConfig.search}}
        if ((params as { search?: string } | undefined)?.search) query.search = (params as { search?: string }).search
{{/if}}
        return unwrap(await $fetch(base, { query, headers: forwardHeaders, credentials: 'include' }))
      } catch (err) {
        fail(err)
        throw err
      } finally {
        loading.value = false
      }
    },
{{/if}}
{{#if (includes operations "create")}}
    create: async (data: Create{{pascalCase entity}}Input): Promise<{{pascalCase entity}}> => {
      const parsed = safeParse(Create{{pascalCase entity}}Schema, data)
      if (!parsed.success) {
        const msg = parsed.issues?.[0]?.message ?? 'Invalid input'
        const err = new Error(msg)
        fail(err)
        throw err
      }

      loading.value = true
      error.value = null
      try {
        const csrfToken = getCsrfToken()
        return unwrap(await $fetch(base, {
          method: 'POST',
          body: parsed.output,
          headers: { ...forwardHeaders, ...(csrfToken ? { 'x-csrf-token': csrfToken } : {}) },
          credentials: 'include',
        }))
      } catch (err) {
        fail(err)
        throw err
      } finally {
        loading.value = false
      }
    },
{{/if}}
    get: async (id: number): Promise<{{pascalCase entity}}> => {
      loading.value = true
      error.value = null
      try {
        return unwrap(await $fetch(`${base}/${id}`, { headers: forwardHeaders, credentials: 'include' }))
      } catch (err) {
        fail(err)
        throw err
      } finally {
        loading.value = false
      }
    },
{{#if (includes operations "update")}}
    update: async (id: number, data: Update{{pascalCase entity}}Input): Promise<{{pascalCase entity}}> => {
      const parsed = safeParse(Update{{pascalCase entity}}Schema, data)
      if (!parsed.success) {
        const msg = parsed.issues?.[0]?.message ?? 'Invalid input'
        const err = new Error(msg)
        fail(err)
        throw err
      }

      loading.value = true
      error.value = null
      try {
        const csrfToken = getCsrfToken()
        return unwrap(await $fetch(`${base}/${id}`, {
          method: 'PUT',
          body: parsed.output,
          headers: { ...forwardHeaders, ...(csrfToken ? { 'x-csrf-token': csrfToken } : {}) },
          credentials: 'include',
        }))
      } catch (err) {
        fail(err)
        throw err
      } finally {
        loading.value = false
      }
    },
{{/if}}
{{#if (includes operations "delete")}}
    remove: async (id: number): Promise<{ ok: boolean }> => {
      loading.value = true
      error.value = null
      try {
        const csrfToken = getCsrfToken()
        return await $fetch(`${base}/${id}`, {
          method: 'DELETE',
          headers: { ...forwardHeaders, ...(csrfToken ? { 'x-csrf-token': csrfToken } : {}) },
          credentials: 'include',
        })
      } catch (err) {
        fail(err)
        throw err
      } finally {
        loading.value = false
      }
    },
{{/if}}
  }
}

{{/each}}
