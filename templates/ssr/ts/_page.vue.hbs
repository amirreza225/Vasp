<template>
  <{{componentName}} />
</template>

<script setup lang="ts">
import {{componentName}} from '{{componentSource}}'
definePageMeta({
{{#if isProtected}}
  middleware: 'auth',
{{/if}}
{{#if noLayout}}
  layout: false,
{{else}}
  layout: 'default',
{{/if}}
})
</script>
